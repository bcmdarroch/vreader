function lovr.load()
  -- load book/text

  -- load model
  -- environment = lovr.graphics.newModel(/assets/'')

  -- load audio

  -- load controllers

  -- load page (plane)

end

function lovr.update()
  -- has user moved?

  -- has user

end

function lovr.draw()
  lovr.graphics.cube('line', 0, 0, -2)
  lovr.graphics.print("hello world!", 0, 0, -5)
  -- render environment given user's position in space

  -- if read mode on, render page with in front of camera

  -- render UI

end
